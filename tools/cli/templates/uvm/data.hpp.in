#pragma once

#include "{{MODULE}}_defs.hpp"

// #############################################################################
//  Transaction types for {{MODULE_UPPER}}
//
//  InputData  - stimulus driven by the sequencer to the driver.
//  OutputData - observations captured by the output monitor.
//
//  Fields below are auto-generated 1:1 from the DUT's own ports (excluding clk/rst) --
//  this does NOT decide transaction semantics (when a field is valid, which fields
//  correlate, timing). Add/remove/group fields as your protocol actually needs.
// #############################################################################

struct {{MODULE_PASCAL}}InData : public sfuvm::InputData
{
{{INDATA_BODY}}
};

struct {{MODULE_PASCAL}}OutData : public sfuvm::OutputData
{
{{OUTDATA_BODY}}
};

{{INDATA_FMT}}

{{OUTDATA_FMT}}
